services:
  frontend:
    build: 
      context: ../../frontend
      dockerfile: docker/Dockerfile.dev
    ports:
      - "4200:4200"
    volumes:
      - ../../frontend:/app
      - /app/node_modules
